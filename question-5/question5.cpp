#include <iostream>
#include <cstdio>
using namespace std;

int main()
{
    char input[100];
    cout << "Enter  string that is going to be inserted in the formating: ";
    cin >> input;

    printf(input); // easily bypass this using specifc string formatting letting kow what to expect like

    return 0;
}